services:
  mysql:
    image: mysql:8.4
    container_name: gymtracker-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: devpassword
      MYSQL_DATABASE: gymtracker
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql


volumes:
  mysql-data: